{
  "name": "@skycoin4444/platform-v4",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Modular foundation for the Skycoin production platform v4.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm build && pnpm exec vitest run",
    "test": "vitest run",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
